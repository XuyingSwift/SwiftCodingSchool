#include <stdio.h>

int main()
{
    // the conditional expression written with the ternary operator ?:

    int a = 3;
    int b = 4;
    int z = (a > b) ? a : b;
    printf("%d\n", z);
  
  return 0;
}
